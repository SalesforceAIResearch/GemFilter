# python setup.py develop
from setuptools import setup, find_packages
setup(
    name="my_baseline", 
    version="0.0.1",
    packages=find_packages(),
)
setup(
    name="my_utils", 
    version="0.0.1",
    packages=find_packages(),
)